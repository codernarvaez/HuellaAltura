# Base de datos local del dominio expedientes
DATABASE_URL=file:./geoguard.db

# JWT — misma SECRET_KEY que auth-service (HS256)
SECRET_KEY="tu_clave_secreta_aqui"

# auth-service (identidad + auditoría centralizada)
AUTH_SERVICE_URL=http://localhost:8000
INTERNAL_API_KEY="tu_api_key_interna_aqui"

# Validación de session_token contra auth-service (desactivar solo en tests locales)
SESSION_VALIDATION_ENABLED=true
