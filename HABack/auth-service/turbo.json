{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//", "../turbo.json"],
  "tags": ["backend", "api", "auth"],
  "tasks": {
    "build": {
      "outputs": ["$TURBO_EXTENDS$"]
    },
    "test": {
      "env": [
        "DATABASE_URL",
        "SECRET_KEY",
        "INTERNAL_API_KEY",
        "JWT_SECRET"
      ]
    }
  }
}
