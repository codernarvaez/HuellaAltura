{
  "name": "auth-service",
  "private": true,
  "description": "HABack identity service — orchestrated by Pants, not Turbo",
  "scripts": {
    "ci": "./pants_ci.sh"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
