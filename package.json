{
  "name": "huella-de-altura",
  "private": true,
  "workspaces": [
    "HAFront",
    "mobile",
    "HABlockchain",
    "backend/auth-service",
    "backend/exped-service"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
